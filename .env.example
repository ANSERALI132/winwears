# WIN WEARS — environment template
# Copy to .env and fill in the real values. Commit this file; never commit .env.

# Anthropic API key used by the project's AI tooling (prompts/).
ANTHROPIC_API_KEY=

# Port the backend listens on once it exists.
PORT=3000
