# OS / editor cruft
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*.swp
.vscode/
.idea/

# Working files
tools/tmp/
# Camera originals of the process films: 197 MB, one of them past GitHub's
# 100 MB limit. The site serves the re-encoded copies in frontend/assets/video.
COMPLETE FOTBALL PROCESS/
*.bak
*.log

# Dependencies / build output (none today — the site has no build step)
node_modules/
dist/
.env
.env.*
!.env.example
